import type { LucideIcon } from "lucide-react"

export type NotificationItem = {
  title: string
  avatar: string
  subtitle: string
}

export type ProfileMenuItem = {
  title: string
  url: string
  icon: LucideIcon
}
